# Exa.CLI

Command-line interface for the Exa SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Exa.CLI --prerelease
```

## Usage

```bash
exa --help
exa api --help
```